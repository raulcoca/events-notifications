import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.events.notifications',
  appName: 'events-notifications',
  webDir: 'www'
};

export default config;
